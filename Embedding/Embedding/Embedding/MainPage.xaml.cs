namespace Embedding
{
    // NOTE: The XAML compilation needs to be off for pages that use native controls on them, as per documentation https://docs.microsoft.com/en-us/xamarin/xamarin-forms/platform/native-views/xaml
    // [XamlCompilation(XamlCompilationOptions.Compile)]
    public partial class MainPage
    {
        public MainPage()
        {
            InitializeComponent();
        }
    }
}